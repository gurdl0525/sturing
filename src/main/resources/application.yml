spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:${port}/${DB}
    password: ${password}
    username: ${userName}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none